
$(document).ready(function() {
	
	debug.log('document.ready: /public/js/config/index.js');

	/*
	
	$showAdv = $('<span style="float: right;"><label for="showAdvaced">'+showAdvLabel+'</label><input type="checkbox" id="showAdvanced" /></span>');
	$('#configs-container h2').before($showAdv);
	
	$('#showAdvanced').change(function (event){
		if ( $(this).is(':checked') ) {
			$('form#autoconfigs dd.advanced').parent().find('select, input, textarea').attr('disabled', false);
		} else {
			$('form#autoconfigs dd.advanced').parent().find('select, input, textarea').attr('disabled', true);
		}
	});
	
	$('form#autoconfigs dd.advanced').parent().find('select, input, textarea').attr('disabled', true);
	
	*/
	
});